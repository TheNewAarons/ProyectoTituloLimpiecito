import { EstadoListaPevSupPipe } from './estado-lista-pev-sup.pipe';

describe('EstadoListaPevSupPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoListaPevSupPipe();
    expect(pipe).toBeTruthy();
  });
});
