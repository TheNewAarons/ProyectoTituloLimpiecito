import { EstadoCronogramaPipe } from './estado-cronograma.pipe';

describe('EstadoCronogramaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCronogramaPipe();
    expect(pipe).toBeTruthy();
  });
});
